box: wercker/default
build:
  steps:
    - script:
      name: install awscli
      code: |
        sudo pip install awscli

    - script:
        name: version
        code: |
          aws --version

    - validate-wercker-step
